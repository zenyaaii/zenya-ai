{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "npm ci --no-audit --no-fund",
  "buildCommand": "npm run build",
  "crons": [
    {
      "path": "/api/cron/domains-expiring",
      "schedule": "0 9 * * *"
    }
  ]
}
