# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# local env files
.env*.local

# vercel
.vercel

.vercel
.env*.local
.env*.bak
.env.*.testmode.bak


# Ignore generated themes and test folders
zenya-theme-test*
*.zip
report.txt
utils/alpine.js
utils/tailwind.js

# typescript incremental-compile cache (local only)
tsconfig.tsbuildinfo
*.tsbuildinfo

# Claude Code workspace state (session-specific)
.claude/

# MCP server config — contains secrets (Stripe + Supabase tokens)
.mcp.json

# Local scratch / reference checkouts (e.g. tmp-open-design, tmp-v3-theme-check)
tmp-*/

testsprite_tests\tmp\config.json

# Shopify push scratch — base64'd liquid + upsert payloads we hand to the MCP
.bundle.json
.bundle-vars.json
.product-main.b64
.upsert*.json
tsc_recent.txt

# Codex working dir (not committed; tool-managed)
.codex/

# local email render output (dev preview only)
.email-previews/

# Audit reports (generated by /audit)
audit-report/

# Lead-generation toolkit — scraped third-party contact data (PII). Kept local, never committed.
leads/

# Playwright E2E — reports, traces, saved session, and test-mode secrets
/playwright-report/
/test-results/
/playwright/.cache/
/tests/e2e/.auth/
.env.test
