// Compatibility shim — typography presets moved to the shared editor
// library so every brochure theme can use them. Keep this re-export so
// older restaurant-side imports keep working.
export * from '../theme-editor-typography'
